{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "GenerateGrpc",
      "type": "shell",
      "command": "docker run --rm --volume \"./:/workspace\" --workdir /workspace bufbuild/buf generate --include-imports --include-wkt",
      "group": "build",
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
